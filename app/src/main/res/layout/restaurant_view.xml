<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="250dp"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/image_view_restaurants"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    
    <View
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignBottom="@id/image_view_restaurants"
        android:background="@drawable/image_shade" />
    <TextView
        android:id="@+id/text_view_restaurants"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="2"
        tools:text="restaurants"
        android:ellipsize="end"
        android:layout_alignBottom="@id/image_view_restaurants"
        android:textColor="@android:color/white"
        app:drawableStartCompat="@drawable/fav_icon"
        android:layout_margin="8dp"/>
</RelativeLayout>